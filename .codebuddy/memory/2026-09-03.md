# 2026-09-03（补充记录3）

## 杂事记录：报销金额自动解析 + 记住上次人名
`pages/misc-records/misc-records.vue`：
- **报销不拆结构化**（保持整段 desc 文字），仅自动解析金额（**总价**，不乘数量）
- 新增 `extractAmountFromText(text)`：优先 `数字+元|块钱|元整`，其次行尾数字；无则 null。已测 8 种写法全过，含「买1个气阀 100 / 100元」→100
- `watch(() => form.desc)`：报销类型下内容变化，若金额框为空则自动填解析值（不覆盖手输）
- `saveRecord`：报销金额未填/无效时从 desc 兜底解析
- **记住人名**：key `misc_last_person`，`openAdd` 默认读上次姓名；`saveRecord` 保存后 `setStorageSync` 记住
- 金额框 placeholder 改「如：100」，报销 label 提示可留空自动识别
- import 增加 `watch`

# 2026-09-03（补充记录2）

## order-list 编辑订单弹框补显示订单日期
`pages/order-list/order-list.vue`：编辑订单 modal 副标题原本是旧的「仅修改当前订单展示信息和单价」，未显示日期 → 已改
- 新增 `editorOrderDate` ref，`openEditModal` 里设 `order?.createdDate`，`closeEditModal` 清空
- 副标题改为 `订单日期：{{ editorOrderDate }}`
- 至此 3 处「编辑订单」弹框全部显示所属日期：stats、user-monthly-summary、order-list

# 2026-09-03（补充记录）

## 删除 Excel 导出失败弹框
`common/export-excel.js`：
- `showExcelPreviewShareActions` 和 `showExcelExportActions` 两个函数 catch 里的「导出失败」`uni.showModal` 全部删除，改为静默（注释"失败静默处理"）
- 已确认全文件无「导出失败」/「文件操作失败」弹框残留

## 按人对账：同一天多笔订单不再合并
`pages/user-monthly-summary/user-monthly-summary.vue` 的 `monthlyGroups`：
- 删除按 `createdDate` 的 `mergedOrdersMap` 合并逻辑
- 改为每笔原始订单独立成一条：`{ _id(真实id), createdDate, createdAt, userName, settlementType, unitPrice:getUnitPrice(order), quantity, returnedBuckets, actualAmountReceived, totalAmount, notes, sourceOrders:[order] }`
- 排序：先按日期倒序，同日期按 createdAt 倒序
- **顺带修复**：之前合并用 `_id: merged-月-日期` 假 id，现在用真实 `_id`，编辑/删除能正确定位单笔订单
- 模板清理：删除「N 笔合并」标签和 `sourceOrders.length===1` 条件；编辑按钮 `openEditModal(ord)` 直接传当前订单
- `diffSegments`/`canParseRemark` 仍逐条计算
