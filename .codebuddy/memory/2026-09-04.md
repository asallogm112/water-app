# 2026-09-04

## 杂事记录：报销去金额框纯解析 + 修复导出金额重复
`pages/misc-records/misc-records.vue`：

1. **报销不显示金额输入框**（纯自动解析）：类型=报销时模板只显示「报销内容（自动提取金额）」框；`type=工资`才显示工资金额框+工资说明。
   - saveRecord：报销 `amount = extractAmountFromText(desc)`，工资用 `form.amount`
   - 报销无内容或内容无金额则报错提示「报销内容里找不到金额，如：电风扇 150 元」
   - 删除旧的 `watch(form.desc)`（不再需要自动填金额框），watch import 也移除
2. **修复导出金额重复**（如「买电风扇 150元 150元」）：
   - 报销明细拼接改为：`alreadyHasAmount = /元|块钱/.test(desc) || /\d\s*$/.test(desc)`（desc 含金额单位**或已以数字结尾**）→ 不重复补金额，只 `${name} ${desc}`；否则才补 `${name} ${desc} ${amount}元`
   - 验证：`张三 电风扇 150 元` 单次；`李四 买螺丝 20` 不补；`王五 买垫片` 无金额才补 `25元`
   - extractAmountFromText 已测含「买1个气阀 100 / 100元」等
3. 记住人名 LAST_NAME_KEY=misc_last_person（openAdd 默认读，save 记住）
