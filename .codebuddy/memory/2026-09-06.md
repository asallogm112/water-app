# 2026-09-06

## 发布 v5.0 到 GitHub
- commit `3c21836 工资和报销功能 OK`，tag `v5.0`，已推送 origin main
- 注意：本机代理 `HTTP_PROXY/HTTPS_PROXY=http://127.0.0.1:7890`（Clash 未开时 push 报 "Failed to connect to 127.0.0.1:7890"）；绕过方法：`env -u http_proxy -u https_proxy -u HTTP_PROXY -u HTTPS_PROXY git push ...`（不动 git config）
- `.codebuddy/` 不纳入提交（git add -A 后 `git reset -- .codebuddy` 排除）

## 删除旧版报告预览页面
- 删 `pages/stats-old-preview/stats-old-preview.vue`、pages.json 注册、home.vue 临时入口（items.push + pageMap 两处）
- 项目剩 11 页；0 残留引用、pages.json 合法

## stats 数据报告：待付金额为 0 显示绿色
- 客户榜单 item：待付 `item.unpaid === 0` → `.lb-money-paid` 绿色 `#059669`；非 0 保持红色 `.lb-money-unpaid`
- 0 lint

## 杂事页再次澄清修正（pages.json 标题 / 编辑弹窗去 tab / 金额可改）
- pages.json 导航栏标题「杂事记录」→「工资报销」（上次漏了这个，首页入口改了但页面标题没改）
- **编辑弹窗**（点 item 打开）的工资/报销类型 tab 删掉：type-picker-row 加 `v-if="!editingId"`（新增弹窗保留 chip 选类型；批量录入弹窗的同名 class 不受影响）
- 编辑报销金额改为**可修改的独立输入框**（type=digit），saveRecord 编辑报销时 amount=Number(form.amount)，新增报销仍从内容提取
- 教训：pages.json 的 navigationBarTitleText 也是"页面标题"的一部分，改页面名称时必须同步；"删掉 tab"在编辑/新增/批量多个弹窗时要确认是哪一个（本文件有 2 处 type-picker-row）
- 验证：0 lint、括号闭合、pages.json 合法、无样式残留

## 杂事页按用户澄清重构：首页入口「工资报销」+ 去掉 segment + 编辑报销锁金额（金额锁定已被本日最后一条覆盖为可改）
- 用户澄清需求：①首页菜单「杂事记录」→「工资报销」②点进来不要 segment 切换按钮 ③编辑时金额禁止修改，只改事项内容
- home.vue 菜单 name 改「工资报销」（desc 未渲染，模板只显示 name）
- misc-records：**删除整个 segment 切换**（按钮+样式+listType），shownRecords 直接 = monthRecords 全列表（工资+报销混排，misc-type 标签区分紫/橙）
- **编辑报销金额锁定**：saveRecord 里 `original = records.find(editingId)`，报销编辑时 amount 用原记录值（不从 desc 提取）；新增报销才从 desc 提取金额；编辑报销不强制 desc 非空
- 弹窗：报销编辑时显示只读金额「金额（不可修改）¥xx」（.editor-amount-readonly）；label 改「报销内容」；openEdit 撤销拼金额回填逻辑
- 教训：**先弄清需求再动手**——上一轮我猜"改为工资和报销"是改 tab 顺序，实际是首页入口改名+去 segment，改了一堆不需要的（顺序/文案/导出段序）；用户需求模糊时宁可先问
- 验证：listType/list-seg 无残留、0 lint、括号闭合

## 杂事页：类型顺序改「工资→报销」+ 修编辑报销"找不到金额"（已被上条覆盖部分逻辑）
- `typeOptions = ['工资','报销']`、listType 默认 '工资'、openAdd 默认 type '工资'；删除无引用的 typeIndex
- **颜色固定映射不随顺序变**：list-seg/misc-type/type-chip 的 class 用 `t === '报销' ? '0' : '1'`（报销紫、工资橙语义不变）
- 汇总卡：工资合计在前；标题/弹窗副标题文案改「工资报销」
- 导出：工资段排到报销段前（rowIndex 累计逻辑同步调整），标题/文件名/sheet 名改「工资报销」
- **bug 根因**：批量录入报销 desc 存的是剥离金额的纯事项，编辑保存时 `extractAmountFromText(desc)` 提取不到 → 报"找不到金额"
- **修复**：openEdit 报销时若 desc 提取不到金额且原记录 amount>0，自动拼 `${desc} ${amount}元` 回填显示
- 验证：typeIndex 无残留、0 lint、括号配对平衡

## 全站 UI 风格统一优化（纯视觉，零逻辑改动）
以 home/order-list/customer-list 为基准 token，全站统一：
- **背景**：App.vue page 底色 `#f7f8fa`→`#f8fbfa`；batch-order-add/user-add 渐变底 `#f1f5f9`→`#f2f6f9`
- **主渐变统一** `#0f766e→#10b981`：user-add/customer-list parse-btn 的 `#14b8a6` 及阴影 `rgba(20,184,166,.14)` 全部换掉
- **弹窗按钮规格统一** h44/r16(删除钮r14)/fs14/w800+阴影：stats、misc（user-monthly 原本就是基准）
- **大表单弹窗遮罩统一** `rgba(15,23,42,.52)`：stats、user-monthly、misc（原来 .42）
- **日期切换按钮统一**（#fff/#e2e8f0/h34/r12/外阴影 `0 6px 16px`）：stats、user-monthly、misc 的 shift-btn + 月份显示框（user-monthly filter-picker、misc editor-month-display，去掉 inset 阴影）
- **字号体系**：summary-title 14px（stats/user-monthly/misc，原 10px）、section-title 10px（user-monthly/misc，原 8px）、弹窗标题 16px（stats/user-monthly/misc/customer-list/batch-order-add，原 15px）、关闭钮 11px/w800/r999（原 12px/w700/r10）
- **输入框统一** #eee/r14：stats editor-input、user-monthly remark textarea、misc editor-input + parse-name-input
- **home**：内容 padding 16px、menu-card r18/阴影 22px、empty-btn 改主渐变
- **order-list**：详情卡左右边距 12px→16px（detail-card margin、order-list padding）
- **misc 杂事**：remark 保存钮纯色 #059669→主渐变（user-monthly L1594）
- **补全空图标**：user-add、batch-order-add 成功横幅空 success-icon 补 `✓`
- **batch-order-add**：parse-label-hint 7px→9px（太小不可读）
- 0 lint；未动 login（独立设计页）、未清死样式（有风险）、未碰任何逻辑

## 杂事报销导出：事项列不再带金额
- 需求：报销导出时"事项"列不要金额（金额已有独立"金额(元)"列，避免重复）
- `buildCombinedSheetRows` 报销 forEach：去掉自动在 desc 后拼 `xx元` 的逻辑
- 改为 `itemText = stripAmountText(rec.desc)`（剥离存储里可能残留的金额文字，兼容单条录入存的含金额 desc），为空显示"—"
- 工资段不变（事项固定"工资"）
