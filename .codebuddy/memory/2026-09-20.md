# 2026-09-20

## 发布 v11.0
- commit `e851fc0 全量 同步刷新数据`（7 files，185 insertions），tag `v11.0`，已推送 origin main（2e58fcc..e851fc0）
- 含：全量同步刷新（refreshAll）、幂等加载去重复请求、按人对账 2×2 布局修复、重复检测改为仅红色提示、杂事页分组与报销锁姓名
- 推送命令用代理 7892（见 MEMORY.md）；`.codebuddy` 工作记忆一并提交

## 首页刷新按钮 = 全量同步数据库
- 首页右上角 ↻ 按钮（`handleRefresh`）原本已调 `loadAllData()`（真实请求：客户 + 订单，2 次云函数调用）
- 新增 store 方法 `refreshAll()`：**清空幂等缓存**（`bootstrapLoaded`/`mergeStatusCache`/`miscRecordsLoaded` 全置回）→ 再 `loadAllData()`；已导出
- `home.vue` 改用 `refreshAll()`，失败时 toast 提示"同步失败，请稍后重试"（成功仅转圈动画反馈，不弹提示）
- 效果：点刷新 → 强制拉最新客户+订单并写入本机（`persistSessionState`），月度账单/数据报告/按人对账等读同一份 state 自动更新；工资报销的同步标记被重置，下次进入该页会重新同步一次
- 0 lint、括号 OK、store.js 行尾无污染

## 消除重复云端请求（uniCloud 按量计费，用户强烈关注）
**问题**：页面里的"已加载"标记是**组件内 ref**（`dataLoaded`/`hasSyncedOnce`），`navigateTo` 进页面组件会重建 → 标记失效 → **每次进入都重新请求云端**：
- `order-list`：每次进入请求 2 次（`loadAllData` + `getOrderMergeStatusList`）
- `misc-records`：每次进入请求 1 次（同步工资报销）→ 且可能反复弹**黑色 toast**（"已同步 N 条本机记录到云端"，`icon:'none'` 的黑底白字就是用户说的"黑色加载框"）

**修复（store 层幂等，App 运行期间只请求一次）** `common/store.js`：
- 新增模块级标记 `bootstrapLoaded` / `mergeStatusCache` / `miscRecordsLoaded`
- 新增 3 个幂等方法并导出：`ensureBootstrapLoaded()`、`ensureMergeStatusList()`、`ensureMiscRecordsLoaded()`（命中缓存返回 `{success:true, cached:true}`）
- `loadAllData` 成功时置 `bootstrapLoaded = true`；`getOrderMergeStatusList` 成功写 `mergeStatusCache`；`loadMiscRecords` 成功置 `miscRecordsLoaded = true`
- `setOrderMergeStatus` 成功后清 `mergeStatusCache`（写操作后允许重新拉取）
- ⚠️ store.js 是**混合行尾**（110 CRLF + 其余 LF），本轮用 python 二进制插入，行尾零污染（+33 行）

**页面改造**：
- `order-list.vue`：onShow 改调 `ensureBootstrapLoaded()`；`loadMergeStatusMap` 改调 `ensureMergeStatusList()`
- `misc-records.vue`：删除组件内 `hasSyncedOnce`，onMounted 直接 `syncRecordsFromCloud()`；内部改用 `store.ensureMiscRecordsLoaded()`，`result.cached` 时直接 return（用本机数据）

**保留真实请求的场景**：写操作后刷新（addOrder/updateOrder 后的 loadOrders）、首页下拉刷新 `loadAllData()`、`setOrderMergeStatus` 后的重新拉取 —— 用户主动或数据变更时才请求
**其他页面**（customer-list / monthly-summary / user-monthly-summary / stats）本来就只用本地 store 数据，无云端请求
- 验证：0 lint、括号 OK、行尾无污染

## 漏删的解析 toast：`parseBatch` 末尾「检测到 N 条重复，已禁止录入」
- 用户点"开始智能解析"仍弹出"禁止录入"提示 → 来源是 `parseBatch` 末尾残留的 `uni.showToast({title:'检测到 N 条重复，已禁止录入'})`（前几轮改造时漏删）
- 已删除该 toast，并删除无引用的 `duplicateBatchItems` computed
- 全文件复核：`禁止` 出现 0 次；解析/提交流程只剩必要提示（未填内容、缺姓名、解析失败、云端同步失败）
- 重复检测最终形态：**仅解析结果里重复项显示红色文字**（`.result-preview-line-duplicate`），无标签、无汇总、无弹窗、无拦截
- 验证：0 lint、view 59/59、括号 OK

## 重复提示最终形态：只有红色文字，无任何标签/说明
- 用户最终要求：去掉「本次重复，请确认 / 本月已存在，请确认」标签，**重复项只把预览文字显示为红色即可**
- 模板删掉 `.batch-dup-hint` 那行 `text`（样式保留未用）；`.result-preview-line-duplicate` 红色文字保留
- 至此重复检测只剩：解析后重复项红色字体（无标签、无汇总、无拦截、无弹窗）
- 0 lint、view 59/59、括号 OK

## 重复检测最终定案：只在解析结果红色显示，其余全部移除
- 用户明确："解析后红色字体显示就行了，其他别管"
- **保留**：解析结果里重复项的红色预览文字（`.result-preview-line-duplicate`）+ 红字标签「本次重复，请确认 / 本月已存在，请确认」
- **移除**：底部汇总提示行（`.batch-dup-summary` 的模板使用）、`saveRecord` 里整段单条重复检测（含弹窗确认 `duplicateKey`/`duplicateRecord`）
- 提交按钮不拦截（按 `batchItems.length` 全部可提交）
- 验证：6 项检查全 PASS、0 lint、view 59/59、括号 OK
- **经验**：用户要的是"提示"而非"管控"，不要把提示做成拦截/弹窗/汇总，越简单越好

## 重复检测改为「只提示不拦截」
- 用户要求：**不要直接拒绝**，只在**解析结果里红色提示**，由他自己判断（因为事项无法准确检测重复）
- 批量录入：
  - 删除 `submittableBatchItems` computed（原来过滤掉重复项）
  - 提交按钮改为按 `batchItems.length`（全部可提交），`submitBatch` 用 `batchItems.value`
  - 重复项文案「本次重复，已禁止 / 本月已存在，已禁止」→「本次重复，请确认 / 本月已存在，请确认」
  - 汇总提示「检测到 N 条重复，已禁止录入」→「检测到 N 条疑似重复（红色），请自行确认后再提交」
  - 去掉 `skipped` 逻辑，toast 只提示「已录入 N 条」，总是 closeBatch()
- 单条保存：检测到疑似重复不再 return，改为 `uni.showModal` 确认（标题「疑似重复」，按钮「仍要保存 / 取消」），确认后照常保存
- 红色提示样式 `.batch-dup-hint` / `.batch-dup-summary` 保留
- 验证：8 项检查全 PASS、0 lint、view 60/60、括号 OK

## 报销重复检测改为「排除事项」
- 用户反馈按事项判重"很不准确"（物品写法千变万化），要求**把事项相同逻辑排除**
- `buildBatchDedupKey` 改为只取 `type|month|name|amount`（**不再包含** `normalizeItem(desc)`）；desc 形参保留但不用
- 新规则（报销与工资一致）：**同月 + 同类型 + 同人 + 同金额 = 重复**
  - 同人同月同金额但事项不同 → 也算重复（用户要的效果）
  - 不同人 / 不同月 / 不同金额 / 不同类型 → 允许
- `saveRecord` 提示文案改为「这条报销本月已录入（金额相同）」
- `normalizeItem` 保留但已无引用（未删，避免多余改动）
- 注：本轮验证脚本因审批超时未执行，改为直接读代码核对（key 仅 4 字段）

## 报销重复检测：补齐单条保存的检测
- **原有**：批量录入已有智能去重（`buildBatchDedupKey` + `existingRecordKeySet` + `batchItemDuplicateInfo`），重复项标红并禁止提交
- **缺口**：单条保存（`saveRecord`）之前**没有**重复检测 → 已补上
- **规则**（与批量一致）：同月 + 同类型 + 同人 + 同事项（`normalizeItem` 物品核心词，容忍写法差异）+ 同金额 = 重复 → 拒绝保存并 toast
  - 报销提示"这条报销本月已录入（事项和金额相同）"；工资提示"这条工资本月已录入（金额相同）"
  - 编辑时排除自身（`r.id !== editingId.value`），不会误判
- 现状：**不同人**买同样物品 → 允许；**跨月**同样事项金额 → 允许（每月正常复购）
- 云端 `addMiscRecords` 仍不做去重（客户端已过滤，避免云端静默跳过）
- node 模拟 8 组用例全部符合预期、0 lint

## 按人对账：送水/回桶/应收/实收 修复为固定 2 行 2 列
`pages/user-monthly-summary/user-monthly-summary.vue`
- **问题**：月份卡片头部的 4 个统计（送水/回桶/应收/实收）用 `flex-wrap: wrap`，内容宽度不一致时出现"第一行 3 个、第二行 1 个"
- **修复1**：`.month-header-stats` → `display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 4px 12px`（固定 2 行 2 列）
- **修复2**：每笔订单的 4 个字段（发水/回桶/应收/实收）原来是 `.order-item-row` 两行 flex + `.order-item-cell-half { width: calc(50% - 3px) }`，改为单个 `.order-item-grid` grid 2 列（`minmax(0,1fr)` 防止长金额撑破），模板合并两个 row 为一个容器
- 删除无引用的 `.order-item-row`、`.order-item-cell-half` 样式
- 验证：无残留、view 117/117 配对、0 lint
- 经验：小程序里 4 项并排优先用 `grid-template-columns: repeat(2, minmax(0,1fr))`，不要用 `flex-wrap`（内容宽度不一会 3+1）、也不要用 flex + `width: calc(50% - Npx)`
